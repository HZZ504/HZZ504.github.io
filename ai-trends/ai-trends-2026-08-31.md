# AI 动态追踪日报 — Claude Cowork & Claude Code

**日期：** 2026-08-31
**数据来源：** 官方文档 · GitHub Releases · Releasebot · Web · YouTube（检索）· Reddit（受限）

> ⚠️ **本期采集方式说明（连续第二期无一手浏览器数据）：** 本次定时运行中，内置浏览器对 `x.com`、`youtube.com`、`claude.com` 三个域名的导航**全部被拒**（定时任务无人在场批准站点权限），Claude in Chrome 扩展**仍未连接**。因此**本期同样没有任何一手浏览器采集数据**——第三节的观看量/时长、第五节的互动量（💬🔁❤️👁）**一律留空或标注"未实测"，不填任何估算数字**。其余章节基于官方文档、GitHub Release 与 Releasebot，已交叉核对。
>
> ✅ **本期新增一条纠错：** 检索过程中发现一条**广泛流传但已失效**的说法——"Sonnet 5 优惠价 8 月 31 日到期、9 月 1 日涨到 \$3/\$15"。该涨价**已于 8 月 10 日被官方取消**。今天正是那个"原定到期日"，这条过时信息大概率会在今明两天集中冒头。详见「今日焦点」。

---

## 今日焦点：今天不涨价 —— 一条正在被误传的"到期日"

**结论先行：Claude Sonnet 5 的 \$2 / \$10（每百万 input / output token）不是优惠价，是永久价。今天（8/31）不是最后一天，明天（9/1）不涨到 \$3 / \$15。**

时间线值得完整摆一遍，因为混淆正是从中间那一步产生的：

| 时间 | 发生了什么 |
|---|---|
| 2026 年 6 月 | [Sonnet 5 发布](https://www.anthropic.com/news/claude-sonnet-5)，定价 **\$2 / \$10**，官方明确写"**through August 31**"（截至 8 月 31 日） |
| 6 月–8 月初 | 大量二手文章据此推导出"9/1 起标准价 **\$3 / \$15**"，等于**涨价 50%**——这个推导在当时是对的 |
| **8 月 10 日** | 官方 [@claudeai 发推](https://x.com/claudeai/status/2086891169217122586)：**"We're making Claude Sonnet 5's introductory pricing permanent."** 涨价计划**作废** |
| **8 月 31 日（今天）** | 原定到期日抵达。**但价格不变。** |

官方原话（[X](https://x.com/claudeai/status/2086891169217122586)）：

> 我们要把 Claude Sonnet 5 的入门定价变成永久定价。我们在 6 月以每百万输入 token \$2、输出 token \$10 发布 Sonnet 5，有效期至 8 月 31 日，而这个价格将保持不变。

**为什么这条值得单独拎出来讲：**

1. **今天是误传的高发日。** 6 月到 8 月初写的那批"9/1 涨价"文章，标题里带着一个具体日期，今天正好到期——搜索引擎和聚合站会把它们重新推到前排。**有些仍在流通的信源（包括本次检索命中的一条）到今天还在按旧口径描述。**
2. **对做预算的人是实质影响。** 如果你在 8 月按"9 月起成本 +50%"做过测算、或据此准备降级到更便宜的模型 / 迁移到别家，**那份测算的前提已经不成立**，值得回去改。
3. **这是一个可复用的核查习惯：带日期的价格承诺，要查有没有后续覆盖。** 官方的原始公告（6 月）和最新状态（8 月）是两个不同的页面，前者不会自动更新。**以 [platform.claude.com 的 Pricing 页](https://platform.claude.com/docs/en/about-claude/pricing) 为准，而不是任何一篇带日期的博客。**

> 📌 **核查方式说明：** 本条经三处交叉核对——官方 X 账号原文、官方定价文档、以及第三方报道（[explainx.ai](https://explainx.ai/blog/anthropic-sonnet-5-permanent-pricing-august-2026) 明确写为"Locked at \$2/\$10"）。同时本次检索也命中了仍持旧口径的信源（[datafloq](https://datafloq.com/anthropic-confirms-claude-sonnet-5-prices-rise-50-on-september-1/) 标题仍为"Prices Rise 50% on September 1"），故在此标注冲突。

---

## 第二焦点：版本节奏停了三天，正好用来消化 8/28 那两个大版本

**Releasebot 上 Claude Code 的最高版本仍是 [2.1.251](https://github.com/anthropics/claude-code/releases/tag/v2.1.251)，首次收录于 8 月 28 日；8/29、8/30、8/31 三天没有新版本收录**（Releasebot 页面自称 "Last updated: Aug 28, 2026"）。

> ⚠️ **口径提醒：** Releasebot 的版本列表**有跳号**（2.1.249、2.1.250、2.1.244、2.1.240–242 均未被单独收录）。所以"没有新条目"≠"官方没发版"，只能说明**没有被这个聚合源索引到**。要确认官方发版情况，应以 [anthropics/claude-code releases](https://github.com/anthropics/claude-code/releases) 与 [官方 changelog](https://code.claude.com/docs/en/changelog) 为准。

节奏空窗期正好适合把 8/28 的两个版本真正用起来。下面把**上期只列了清单、没讲怎么配**的三件事补成可操作的配置。

---

## 一、高级用法精选（按难度分层）

### Level 1 — 基础配置：`CLAUDE.md` 是每次会话的第一份输入

`CLAUDE.md` 在每次会话开始时被读取，是你的常驻指令文件。适合放：项目结构约定、文件路径、代码风格、语气要求、行为护栏。

> ⚠️ **一条安全提醒（与本项目 CLAUDE.md 相关）：** 网上不少教程建议把 **API key 写进 `CLAUDE.md`**。**不要这么做。** 该文件通常随仓库提交，密钥应走环境变量或密钥管理器。这是流传较广的一条坏建议。

### Level 2 — 输出控制：Concise 输出风格 + `/design`

8 月的 Claude Code 加了 **Concise 输出风格**（结论先行，不铺垫）与 **`/design` skill**（把一个想法或一张截图变成可编辑界面）。对话冗长是最常见的 token 浪费源，Concise 是最低成本的一刀。

### Level 3 — 权限即句子：auto 模式成为默认

允许 / 拒绝规则现在可以**用自然语言句子写**，auto 模式成为新默认。这降低了 `settings.json` 里手写 glob 的门槛。

### Level 4 — `--restricted`：可以放进定时任务的"只读 Claude"

来自 **2.1.248**。一个 flag（等价环境变量 `CLAUDE_CODE_RESTRICTED=1`）一次性做四件事：

- 移掉所有能执行命令或代码的内置工具，以及 `WebFetch`（除非在 `--tools` 里显式点名）
- 文件工具锁死在工作目录内
- 拒绝 `bypassPermissions`
- **忽略 user / project / local 三层设置文件**

最后一条是精髓：**受限模式不信任仓库里的配置**。处理一个不完全信任的代码库时，对方 `.claude/settings.json` 里写什么都不算数。

```bash
# 审阅一个来路不明的仓库：只读，不执行，不读它的配置
claude --restricted -p "总结这个仓库的架构，列出所有网络请求发起点"
```

### Level 5 — Hooks：`PreModelSwitch` / `PostModelSwitch`

来自 **2.1.251**。新增两个生命周期事件，可以**阻止、确认或标注一次模型切换**——为受控灰度（controlled rollout）而设计。

配置结构是一棵树：顶层 `hooks` 键把**事件名**映射到**匹配器数组**，每个匹配器把一个 matcher 字符串配上一组 handler。团队共享的项目应放在 `.claude/settings.json`，让 hook 规则随仓库走、和代码一起过 review。

```jsonc
// .claude/settings.json —— 结构示意，字段以官方 hooks 文档为准
{
  "hooks": {
    "PreModelSwitch": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": ".claude/hooks/confirm-model-switch.sh" }
        ]
      }
    ]
  }
}
```

> ⚠️ **一个容易踩的坑：`PreModelSwitch` 上，hook 因超时被取消 = 阻止这次切换。** 超时的默认行为是"拦"而不是"放"。写脚本时要保证它能快速返回，否则你会得到一个莫名其妙切不动模型的 Claude。

同版本还给 **`SessionStart` 的 resume hook** 加了两个入参：**会话陈旧度（staleness）** 与 **预估重新缓存成本（re-cache cost）**。这让"要不要恢复这个旧会话"从拍脑袋变成可判断——旧到一定程度、重建缓存贵到一定程度，就该开新会话。

完整的 30 个生命周期事件见 [官方 hooks 文档](https://code.claude.com/docs/en/hooks)。

### Level 6 — 子代理编排 + 跨会话通信

**跨会话消息（cross-session messaging）** 落地于 **2.1.248**：一个会话用 `ListAgents` 发现你的其他会话，用 `SendMessage` 投递一段短文本；接收方在两次工具调用之间读取，空闲时则直接开新一轮。

- **平台限制：macOS 与 Linux（含 WSL 2）可用，原生 Windows 不可用。** v2.1.224 及以后默认开启，无需配置。
- **权限握手有对称规则：** 每个会话用 `crossSessionInbound` 设置检查入站消息（accept / hold / refuse）。**一个绕过权限提示的会话，会把来自"会提示"会话的每条消息扣下等你批准；反过来，一个"会提示"的接收方也会扣下来自"绕过"发送方的消息。** 设计意图是不让权限级别通过消息通道被隐式提升。
- **⚠️ 一个非直觉的关闭条件：** `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`、`DISABLE_TELEMETRY`、`DO_NOT_TRACK`、`DISABLE_GROWTHBOOK` 中**任意一个**都会关掉该特性的判定，从而**禁用跨会话消息**。如果你在企业环境里设过这些变量，功能不生效不是 bug。

**组合出的模式：`--restricted` 的多个 worker + 一个有权限的 foreman。** 受限 worker 负责读和分析（碰不到执行工具、读不到仓库配置），foreman 汇总后执行写操作。安全边界和职责边界第一次对齐了。

### Level 7 — 成本可观测：把缓存命中率变成运维指标

**2.1.251** 给 `/cost` 加了**每会话的 prompt-cache 行**：命中率、miss 数、重新缓存的 token 数、warm / cold 状态，并配了同构的 `prompt_cache` 对象供状态栏脚本读取。`/usage` 则加了 **Spend limit 进度条**，以及给网关后开发者用的 `rate_limits.spend_limit` 状态栏字段。

这是把"我这个月为什么花这么多"从月末对账**前移到了实时状态栏**。缓存命中率低通常意味着你在频繁改动 prompt 的前缀部分——那是可以重构的。

**后台会话管理也补齐了：** CLI help 现在包含 `attach`、`logs`、`stop`、`respawn`、`rm`；对一个正在运行的后台会话执行 `--resume`，提示里会**直接给出 `claude attach <id>` 的完整命令**，不用再去别处查 id。

---

## 二、高手实战技巧表格

| # | 技巧 | 说明 | 难度 | 来源 |
|---|---|---|---|---|
| 1 | `!` 直通 shell | 输入 `!` 后接命令，直接在 shell 执行，Claude 不解释 | ★☆☆ | [AI Coding Daily](https://aicodingdaily.substack.com/p/claude-code-my-favorite-10-tips-and) |
| 2 | `/export` 存档会话 | 把整段对话导出到文件或剪贴板 | ★☆☆ | [AI Coding Daily](https://aicodingdaily.substack.com/p/claude-code-my-favorite-10-tips-and) |
| 3 | Concise 输出风格 | 结论先行，砍掉铺垫，最低成本的省 token 手段 | ★☆☆ | [origami.sa](https://origami.sa/en/blog/claude-code-august-2026/) |
| 4 | 权限写成句子 | auto 模式下用自然语言写 allow / deny，替代手写 glob | ★★☆ | [origami.sa](https://origami.sa/en/blog/claude-code-august-2026/) |
| 5 | `/design` 从截图生界面 | 一个想法或一张截图 → 可编辑界面 | ★★☆ | [origami.sa](https://origami.sa/en/blog/claude-code-august-2026/) |
| 6 | `--restricted` 审阅陌生仓库 | 只读 + 无执行 + 忽略仓库配置，安全消费第三方代码 | ★★★ | [Releasebot 2.1.248](https://releasebot.io/updates/anthropic/claude-code) |
| 7 | `/cost` 看缓存命中率 | 命中率 / miss / 重新缓存 token / warm-cold，实时成本归因 | ★★★ | [v2.1.251](https://github.com/anthropics/claude-code/releases/tag/v2.1.251) |
| 8 | `claude attach <id>` 接管后台会话 | `--resume` 提示里直接给全命令，配合 `logs` / `stop` / `respawn` / `rm` | ★★★ | [v2.1.251](https://github.com/anthropics/claude-code/releases/tag/v2.1.251) |
| 9 | `SessionStart` resume 拿陈旧度与重缓存成本 | 用数据决定"恢复旧会话还是开新的" | ★★★★ | [v2.1.251](https://github.com/anthropics/claude-code/releases/tag/v2.1.251) |
| 10 | `PreModelSwitch` 卡模型切换 | 受控灰度；注意超时=阻止 | ★★★★ | [hooks 文档](https://code.claude.com/docs/en/hooks) |
| 11 | `ListAgents` + `SendMessage` 跨会话协作 | 受限 worker + 有权限 foreman 的编排模式 | ★★★★★ | [跨会话消息文档](https://code.claude.com/docs/en/cross-session-messaging) |
| 12 | Skills 优于 Projects | Skill 按上下文触发，只在相关时激活，不占常驻预算 | ★★★ | [No Code MBA](https://www.nocode.mba/articles/claude-cowork-tips) |

---

## 三、YouTube 教程推荐

> ⚠️ **本期未做浏览器实时采集**（内置浏览器对 `youtube.com` 导航被拒，Chrome 扩展未连接）。下表由 **WebSearch 检索**得到，**观看量、时长、章节信息一律未实测，留空**。发布时间仅在检索结果明确给出时才填写。

| 视频标题 | 作者 | 观看量 | 时长 | 特色内容 |
|---|---|---|---|---|
| [How to Use Multi-Agent Workflows in Claude Code: Skills, Subagents & Handoffs (2026)](https://www.youtube.com/watch?v=qiDalcMeBFk) | 未实测 | 未实测 | 未实测 | 多代理编排三件套：Skills / Subagents / Handoffs；检索结果标注发布于 2026-07-26 |
| [Claude Code Advanced Workflow Tutorial (Slash Commands, Subagents & Hooks)](https://www.youtube.com/watch?v=6rdQXdEy-8U) | 未实测 | 未实测 | 未实测 | 斜杠命令 + 子代理 + hooks；检索结果标注发布于 2026-01-08，**内容早于 8 月的 hooks 新事件，参考时注意版本差** |
| [The ONLY Claude Cowork Tutorial You'll Ever Need in 2026](https://www.youtube.com/watch?v=PnPsf-hwGtI) | 未实测 | 未实测 | 未实测 | Cowork 入门通盘教程 |

**检索入口（可自行核实实时数据）：**

- [YouTube 搜索：Claude Code tips tricks 2026](https://www.youtube.com/results?search_query=Claude+Code+tips+tricks+2026)
- [YouTube 搜索：Claude Cowork tutorial 2026](https://www.youtube.com/results?search_query=Claude+Cowork+tutorial+2026)

---

## 四、Reddit 讨论精选

> ⚠️ Reddit 因浏览器安全限制无法直接访问，且目前无可用的 Reddit MCP connector。本期 WebSearch **未命中任何 2026 年 8 月的 [r/ClaudeAI](https://www.reddit.com/r/ClaudeAI/) 具体帖子**——检索返回的均为 Substack / DEV / 博客等二手汇编，而非 Reddit 原帖。
>
> **因此本节本期为空，不填充任何无法追溯到原帖的"讨论摘要"。** 这比编造一个看似合理的帖子列表更诚实。

**替代信源（本期实际参考的社区向汇编）：**

| 来源 | 类型 | 要点 |
|---|---|---|
| [Claude Cowork Guide for Power Users: 50+ Tested Tips](https://karozieminski.substack.com/p/claude-cowork-guide-plugins-memory-sub-agents-tips) | Substack 长文 | 插件 / Skills / 子代理 / 记忆，作者标注为"实测" |
| [Claude Code Advanced Best Practices: 11 Practical Techniques](https://smartscope.blog/en/generative-ai/claude/claude-code-best-practices-advanced-2026/) | 技术博客 | hooks / 子代理 / 上下文管理三块 |
| [Claude Code subagents: the 2026 production playbook](https://www.totalum.app/blog/claude-code-subagents-totalum) | 技术博客 | 子代理的生产环境用法与示例 |
| [Claude Code Hooks Complete Guide](https://hidekazu-konishi.com/entry/claude_code_hooks_complete_guide.html) | 技术博客 | 逐个 hook 事件讲触发点与回传通道，附可复制示例 |

**待恢复的检索入口：**

- [r/ClaudeAI](https://www.reddit.com/r/ClaudeAI/) · [r/ClaudeCode](https://www.reddit.com/r/ClaudeCode/)

---

## 五、X 热门帖子

> ⚠️ **本期未做浏览器实时采集**（内置浏览器对 `x.com` 导航被拒，Chrome 扩展未连接）。**互动数据（💬 🔁 ❤️ 👁）一律未实测，留空，不填任何估算值。** 下表仅收录通过 WebSearch 明确命中、可追溯到具体推文 URL 的条目。

### Claude 官方 / 变更日志

| 作者 | 内容摘要 | 互动 | 亮点 |
|---|---|---|---|
| [@claudeai](https://x.com/claudeai) | [宣布 Sonnet 5 入门定价永久化](https://x.com/claudeai/status/2086891169217122586)：\$2 / \$10 保持不变，取消 9/1 涨到 \$3 / \$15 的计划 | 未实测 | **本期最重要的一条**，直接作废了 6–8 月大量二手文章的结论 |
| [@ClaudeCodeLog](https://x.com/ClaudeCodeLog) | [2.1.251 亮点串](https://x.com/ClaudeCodeLog/status/2093407907446444049)：71 项 CLI 变更，`PreModelSwitch` / `PostModelSwitch` 用于受控灰度；文件工具阻止工作目录外的符号链接调包 | 未实测 | 唯一持续追踪 changelog 的第三方账号，适合订阅 |
| [@ClaudeCodeLog](https://x.com/ClaudeCodeLog) | [2.1.251 详版](https://x.com/ClaudeCodeLog/status/2093407919714754822)：新增前台子代理工具调用向 Remote Control 客户端的**实时流式传输**（后台子代理为默认，仍只显示状态） | 未实测 | 前台/后台子代理的可观测性差异，是编排设计时的关键取舍 |

### Claude Cowork 热帖

| 作者 | 内容摘要 | 互动 | 亮点 |
|---|---|---|---|
| — | 本期 WebSearch **未命中**可追溯到具体推文 URL 的 Cowork 社区帖 | — | 本节留空，待浏览器恢复后补采 |

**检索入口（可自行核实实时数据）：**

- [X 搜索：Claude Code tips（最新）](https://x.com/search?q=Claude%20Code%20tips&f=live)
- [X 搜索：Claude Cowork（最新）](https://x.com/search?q=Claude%20Cowork&f=live)

---

## 六、跨平台趋势总结

**1. 一次安全修复，暴露了 agent 时代的新攻击面。**

2.1.251 修了一个值得单独讲的漏洞：**文件工具（Read / Write / Edit）此前会跟随一个在权限检查通过之后、被调包进工作目录的符号链接**，从而在批准范围之外读写文件。这是经典的 **TOCTOU（检查时/使用时）** 竞态，只不过受害者从操作系统换成了 AI agent。

它的含义超出这一个 bug：**当 agent 的权限模型建立在"路径"之上，而路径可以在检查后被替换，那么权限检查就是可绕过的。** 这也解释了 `--restricted` 为什么要"忽略仓库里的配置文件"——同一个防御思路：**不信任被审对象提供的任何元数据。**

**2. 三条产品线在同一个方向上收敛：把控制权做成可配置的边界。**

- `--restricted`：**信任边界**（这个 Claude 能碰什么）
- `crossSessionInbound` 的对称握手：**通信边界**（哪个 Claude 能指挥哪个）
- `PreModelSwitch`：**变更边界**（什么时候允许换模型）

8 月上半月的主题是"往外接"（内置浏览器、Claudeforce、MHS 硬件标准），下半月的主题明确转向"给接口装闸门"。**这是一个功能铺开之后的正常回摆，也是企业采购真正会问的那几个问题。**

**3. 成本可观测性正在从"月末对账"变成"实时指标"。**

`/cost` 的缓存命中率、`/usage` 的 spend limit 进度条、状态栏可读的 `prompt_cache` 与 `rate_limits.spend_limit` 字段——这套东西的受众不是个人用户，是**要给团队做成本归因的人**。当 token 消耗能被拆到会话粒度并暴露给脚本，它才第一次成为可以进 dashboard 的运维指标。

**4. 内置浏览器的推广节奏，本机就是一个反例样本。**

官方说 Pro / Max / Team 在一周内陆续到达。**但本机连续两期定时任务中，内置浏览器对目标站点的导航均被拒绝**——原因是无人值守的定时任务无法完成站点权限批准。这不是功能缺陷，是**"默认开启的交互式浏览器"与"无人在场的定时任务"之间的结构性冲突**。见第八节。

---

## 七、进阶路线图

| 阶段 | 目标 | 关键动作 | 验收标准 |
|---|---|---|---|
| **第 1 周** | 让输出可控 | 写好 `CLAUDE.md`（**不放密钥**）；打开 Concise；熟悉 `!` 与 `/export` | 单次任务的往返轮数下降 |
| **第 2 周** | 让成本可见 | 用 `/cost` 看缓存命中率；`/usage` 设 spend limit；状态栏接 `prompt_cache` | 能说出自己命中率低的原因 |
| **第 3 周** | 让权限成型 | 用句子写 allow / deny；对陌生仓库一律 `--restricted` | 不再靠 `bypassPermissions` 过日子 |
| **第 4 周** | 让流程自动 | 上第一个 hook（建议从 `PostToolUse` 跑格式化/lint 起步） | hook 能稳定触发且不误拦 |
| **第 5–6 周** | 让任务并行 | 后台会话 + `attach` / `logs` / `stop`；跨会话消息试通 | 能同时推进两条任务线 |
| **第 7 周+** | 让编排成体系 | 受限 worker + 有权限 foreman；`PreModelSwitch` 做受控灰度 | 安全边界与职责边界对齐 |

---

## 八、推荐行动

**立刻做（今天）**

1. **如果你在 8 月做过"9 月起 API 成本 +50%"的测算——回去改掉。** Sonnet 5 的 \$2 / \$10 已永久化，涨价取消。以 [官方 Pricing 页](https://platform.claude.com/docs/en/about-claude/pricing) 为准，别信任何一篇带"9 月 1 日"的旧博客。
2. **升级到 2.1.251。** 它含一个**符号链接调包**的安全修复——文件工具此前可能在批准范围外读写。这条不是可选项。

**本周做**

3. **给"审阅陌生代码"这件事定一条规矩：一律 `--restricted`。** 它忽略仓库自带配置，这正是审阅不可信代码时你需要的。
4. **跑一次 `/cost` 看缓存命中率。** 如果命中率低，检查是不是在频繁改动 prompt 前缀——那是可重构的。
5. **写第一个 hook。** 从 `PostToolUse` 跑 `ruff format` 起步，比直接上 `PreModelSwitch` 稳妥。**记住 `PreModelSwitch` 超时 = 阻止切换。**

**本月做**

6. **试通跨会话消息。** macOS / Linux 可用。**先确认你没设 `DISABLE_TELEMETRY` / `DO_NOT_TRACK` 等变量**——它们会静默禁用该特性。
7. **把"受限 worker + 有权限 foreman"跑一遍。** 这是目前编排范式里安全性最清晰的一种。

**运维教训（连续第二期，需要动手修）**

8. **本日报的一手采集链路已连续两期失效，不能再当偶发处理。** 根因是**结构性的**：内置浏览器"默认开启、按站点交互式批准"的设计，与"无人值守定时任务"天然冲突——没人在场点批准，导航就被拒。三条可行路径：
   - **改跑有人值守的时段**，或在定时任务前手工预批准 `x.com` / `youtube.com` 的站点权限（若权限可持久化）；
   - **保持 Chrome 扩展常连**（本机连续两期显示未连接），走 Claude in Chrome 通道；
   - **接受现状，把日报定位从"一手互动数据"改为"官方源 + 交叉核对"**，并把第三、五节的表格结构相应简化。

   **在修好之前，第三 / 五节的"未实测"标注必须保留——不填估算数字是这份日报唯一的可信度来源。**

---

*本期共核查外部链接 20+ 条；标注数据冲突 1 处（Sonnet 5 定价）；因无法追溯原帖而主动留空 2 节（Reddit 讨论、Cowork X 热帖）。*
